Панель к MTProxyL **v1.5.1**. Обновление: `mtproxyl panel install` либо
раздел **Обновление** в самой панели.

### Исправлено

- **Вариант C «Telegram через WARP» доступен и в реаниматоре.** Раньше кнопка
  отказывалась работать, если конфигом движка владеем не мы. Теперь панель
  поднимает туннель в любом режиме, а для чужой цели показывает в логе
  операции, что дописать в её конфиг.
- Текст про варианты приведён в соответствие: обязательного режима менеджера
  у варианта C больше нет.

### Требуется

MTProxyL **v1.5.1** — панель зовёт его команды `warp`. Обновите сначала его:
`mtproxyl update`.
